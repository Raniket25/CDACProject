import { ClassPackagecomplete } from './class-packagecomplete';

describe('ClassPackagecomplete', () => {
  it('should create an instance', () => {
    expect(new ClassPackagecomplete()).toBeTruthy();
  });
});
